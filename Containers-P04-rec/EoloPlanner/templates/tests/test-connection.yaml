apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "EoloPlanner.fullname" . }}-test-connection"
  labels:
    {{- include "EoloPlanner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "EoloPlanner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
